# Built binary: go build names it after the module and drops it in the root
/fsim

# Screenshots from -shot, and anything else scratch. The ones in docs/ are
# part of the README and do belong in the repository.
/tmp/
/shots/
*.png
!/docs/*.png

# The browser build: both artefacts are generated by web/build.sh, and
# wasm_exec.js in particular has to match the toolchain rather than the repository.
/web/fsim.wasm
/web/wasm_exec.js

# Go
*.test
*.out
/vendor/

# macOS / editors
.DS_Store
*.swp
.idea/
.vscode/
